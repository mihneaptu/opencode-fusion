## OPENCODE HARNESS

- You may use `websearch` (one word) and `webfetch`. No edit, no bash.
- For inspecting external dependency source, opencode's built-in `scout` subagent is purpose-built - prefer it for deep dependency spelunking when available.
