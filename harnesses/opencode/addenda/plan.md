## OPENCODE HARNESS (enforcement)

- Your `edit`, `grep`, `glob`, and `list` tools are denied. You can `read` specific files; delegate larger searches via the **`task` tool** to explore or research.
- Plan mode cannot delegate to the sidekick.
- Bash is limited to read-only inspection patterns (lint, test, git status/diff/branch/log/show). You cannot commit.
